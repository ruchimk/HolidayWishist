<% flash.each do |key, value| %>
<%= content_tag :div, value, class: "flash #{key}" %>
<% end %>

<% if current_user %>
<div id="items" class=“transitions-enabled”>
  <% @items.each do |item| %>
  <div class="box panel panel-default">
    <div class="image"><%= link_to image_tag(item.image_url), item %></div>
    <div class="panel-body">
      <div class="overlay"><a href="<%= item.url %>" target=_blank><%= item.title %></a></div>
    </div>
  </div>
  <% end %>
</div>

<% else %>
<br>
<div class="row">
  <div class="container">
<div class="welcome_message">
<p>Welcome to Holiday Wishlist--We make secret santa easier than it has been ever before! You never have to be a vicitim of regifting ever again (or vise versa)! See what your friends really want for the holidays and share your wishlist with others. You can also add items from Etsy and save them into your wishlist.</p>
<br>
<p>More to come: You will soon be able to:
  <ol>
    <li>Purchase items with the We-Pay or Stripe </li>
    <li>Search for products from a collection of some of my favorite retailers (Jcrew, Anthropology, Nordstorm, etc..)</li>
    <li>See a single page view of all the products</li>
  </ol>
  </p>
</div>
</div>
</div>
<br>
<br>
<br>
<br>
<% end %>




